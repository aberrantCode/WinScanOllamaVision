# Production dependencies
PyQt6
PyMuPDF
Pillow
requests
ollama
httpx
windows-toasts>=1.1.0; sys_platform == "win32"  # winrt-* deps are Windows-only

# Development dependencies
pytest>=9.0.0
pytest-cov>=7.0.0
pytest-asyncio>=1.3.0
pytest-xdist>=3.8.0
ruff>=0.15.22
mypy>=1.13.0
pre-commit>=4.0.0
bandit>=1.8.0
pip-audit>=2.7.0
build>=1.0.0        # Local build-check (python -m build)
twine>=5.0.0        # Local build-check (twine check dist/*)
types-requests  # Type stubs for mypy
